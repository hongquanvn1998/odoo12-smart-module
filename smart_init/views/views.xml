<odoo>
  <data>
    <function model="smart_init.apps" name="start_server"/>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="smart_init.list">
      <field name="name">smart_init list</field>
      <field name="model">smart_init.smart_init</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="smart_init.action_window">
      <field name="name">smart_init window</field>
      <field name="res_model">smart_init.smart_init</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="smart_init.action_server">
      <field name="name">smart_init server</field>
      <field name="model_id" ref="model_smart_init_smart_init"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="smart_init" id="smart_init.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="smart_init.menu_1" parent="smart_init.menu_root"/>
    <menuitem name="Menu 2" id="smart_init.menu_2" parent="smart_init.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="smart_init.menu_1_list" parent="smart_init.menu_1"
              action="smart_init.action_window"/>
    <menuitem name="Server to list" id="smart_init" parent="smart_init.menu_2"
              action="smart_init.action_server"/>
    -->
  </data>
</odoo>